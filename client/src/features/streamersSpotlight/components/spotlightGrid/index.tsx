import VideoGrid from "../../../videoGrid/index";

import React from "react";

type Props = {};

function SpotLightGrid({ data }) {
  return <VideoGrid data={data} />;
}

export default SpotLightGrid;
